from pathlib import Path
from docx import Document
from docx.shared import Pt
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn

OUT = Path(r'f:\NLP\新建 Microsoft Word 文档.docx')

def font(run, name='宋体', size=12, bold=False):
    run.font.name = name
    run._element.rPr.rFonts.set(qn('w:eastAsia'), name)
    run.font.size = Pt(size)
    run.bold = bold

def para(doc, text, first=True):
    p = doc.add_paragraph()
    p.paragraph_format.line_spacing = 1.5
    p.paragraph_format.space_after = Pt(0)
    if first:
        p.paragraph_format.first_line_indent = Pt(24)
    r = p.add_run(text)
    font(r)
    return p

def heading(doc, text, level=1):
    p = doc.add_heading('', level=level)
    r = p.add_run(text)
    font(r, '黑体', 14 if level == 1 else 12, True)
    return p


doc = Document(OUT)

doc.add_page_break()
heading(doc, '附录A 项目扩展说明与深度分析')
para(doc, '为了进一步说明本项目与课程要求之间的对应关系，本附录对数据处理、模型实现、训练调试、评价指标和案例分析进行更细致的补充。主报告已经给出了整体实验流程和核心结果，本附录则从实验设计思路和模型行为解释两个角度展开，使报告内容更加完整，也便于答辩时回答为什么这样设计、结果为什么会这样、后续可以如何改进等问题。')

heading(doc, 'A.1 数据准备过程的补充说明', 2)
for t in [
    '机器翻译任务与普通文本分类任务不同，它要求训练样本必须是成对出现的平行句子。也就是说，每一条中文句子都需要有一条与之对应的英文译文。如果句子之间没有正确对齐，模型学习到的就不是翻译关系，而是噪声映射。因此，本项目选择 OPUS OpenSubtitles 作为数据来源后，首先关注的是如何把原始的两个文本文件整理为结构化数据。',
    'OPUS 下载得到的中文文件和英文文件本身是逐行对齐的，但在实际处理过程中仍然需要清洗。空行会造成无效样本，过长句会造成训练速度变慢和 padding 增多，重复句会使模型过度记忆少量高频表达，不对齐句子则会直接干扰翻译学习。项目通过转换脚本将原始文本整理为 CSV，并在训练阶段继续进行清洗和划分，保证训练集、验证集和测试集的基本可用性。',
    '本实验将中文作为源语言，将英文作为目标语言。这样设置可以很好地贴合“中英平行语料库”的课程要求。CSV 中使用 zh 和 en 两列，既清晰表达语言方向，也方便通过命令行参数指定列名。若后续更换为其他中英语料，只要保持相同的两列格式，就可以直接复用训练脚本。',
    '中文预处理采用字符级切分，这是一个稳定但相对保守的选择。它的优点是不需要额外安装中文分词工具，也不会因为分词词典缺失而失败；缺点是模型看到的是单个汉字而不是完整词语。例如“机器翻译”会被切分为“机、器、翻、译”，模型需要自己学习这些字符组合的语义。对于课程实验而言，这种方法简单可靠；对于实际系统而言，使用 jieba、BPE 或 SentencePiece 通常会更合适。'
]:
    para(doc, t)

heading(doc, 'A.2 模型结构设计的补充说明', 2)
for t in [
    '本项目选择编码器—解码器结构，是因为机器翻译本质上是一个序列到序列的问题。输入的中文句子长度不固定，输出的英文句子长度也不固定，两者之间不存在简单的一一位置对应关系。因此，模型需要先理解整个源句，再逐步生成目标句。Seq2Seq 模型正是为这类问题设计的。',
    '编码器使用双向 GRU 的原因在于，翻译一个词时往往需要同时参考它前后的上下文。单向循环网络只能看到一个方向的信息，而双向结构可以同时从左到右和从右到左读取源句，使每个位置的表示包含更充分的上下文。对于中文句子而言，某些词语的含义依赖后续内容，双向编码可以在一定程度上改善表示质量。',
    '解码器使用 GRU 逐步生成英文译文。每一步生成时，解码器不仅依赖上一步的隐藏状态，也依赖上一步生成的 token。这样可以形成自回归生成过程，即前面已经生成的内容会影响后续输出。该机制能够生成长度可变的句子，但也可能带来误差累积问题：如果前面某一步生成错误，后续生成可能继续偏离正确译文。',
    '注意力机制是本项目的重点之一。没有注意力的 Seq2Seq 模型需要把整个源句压缩到一个固定向量中，长句信息容易丢失。Bahdanau Attention 则允许解码器在每个时间步重新计算对源句各位置的关注程度，使模型可以根据当前要生成的英文词动态选择需要参考的中文部分。这不仅提升了模型表达能力，也使模型具有一定可解释性。'
]:
    para(doc, t)

heading(doc, 'A.3 训练调试过程的补充说明', 2)
for t in [
    '训练阶段的主要目标是让模型最小化预测译文与参考译文之间的差异。本项目使用交叉熵损失函数，并忽略 padding 位置的损失。这样做是因为 padding 只是为了组成 batch 而添加的占位符，并不属于真实句子内容。如果不忽略 padding，模型会把大量精力用于学习无意义的填充标记。',
    '训练损失可以反映模型对训练集的拟合情况，验证损失可以反映模型对未参与训练数据的泛化情况。若训练损失持续下降而验证损失上升，通常说明模型过拟合。本实验中训练损失和验证损失都呈下降趋势，说明模型确实学到了一定规律，并且在三轮训练内没有出现明显过拟合。',
    '由于实验在 CPU 环境下运行，训练轮数和样本规模受到限制。本实验选择 8000 条训练样本、1000 条验证样本和 1000 条测试样本，是在运行时间和实验完整性之间做出的折中。虽然这不足以训练出高质量翻译系统，但已经足够展示损失下降、模型保存、测试评估和案例分析的完整流程。',
    '梯度裁剪是循环神经网络训练中的重要技巧。RNN 或 GRU 在处理较长序列时，梯度可能出现爆炸，导致训练不稳定。本项目设置梯度裁剪阈值为 1.0，可以限制梯度范数，降低训练过程中出现异常震荡的风险。Dropout 则用于减少模型对训练集的过度依赖，提高一定的泛化能力。'
]:
    para(doc, t)

heading(doc, 'A.4 BLEU结果的进一步解释', 2)
for t in [
    '本实验最终 BLEU 为 2.7763，从绝对数值看并不高。需要注意的是，BLEU 是一个严格的 n-gram 匹配指标，它不仅要求模型生成正确单词，还要求词序和连续短语与参考译文相近。对于字幕语料而言，同一句中文可能存在多种合理英文表达，如果模型输出与参考译文含义相近但措辞不同，BLEU 仍然可能较低。',
    '从分项指标看，1-gram precision 明显高于 2-gram、3-gram 和 4-gram precision。这说明模型能够生成部分正确的英文单词，但在组成连续短语和完整句子方面能力不足。这与基础 RNN Seq2Seq 模型的特点一致：它可以学习高频词对应关系，但对长距离依赖、句法结构和自然表达的掌握较弱。',
    'BLEU 低的原因还与数据和训练条件有关。OpenSubtitles 字幕语料口语化强，句子经常省略主语或谓语，有些翻译依赖上下文；本实验只训练 3 轮，且使用字符级中文切分和贪心解码；模型没有使用 Transformer、预训练模型或 Beam Search。因此，BLEU 分数应该与实验条件一起解释，而不能单独作为判断项目是否完成的依据。',
    '对于本课程项目而言，更关键的是模型能否完整跑通，损失是否下降，能否输出注意力图，能否通过案例分析解释模型行为。从这些方面看，本实验已经实现了端到端机器翻译流程，并能够观察模型的优势和失败模式。'
]:
    para(doc, t)

heading(doc, 'A.5 成功与失败案例的深入讨论', 2)
for t in [
    '成功案例主要集中在短句、高频词和常见口语表达中，例如“谁？”、“是的。”、“我知道。”等。这类句子在字幕语料中出现频率高，句法结构简单，中文和英文之间的对应关系直接，因此基础模型较容易学习。注意力图中，这类样本通常表现为较集中的关注区域，说明模型在生成英文词时能够找到相对明确的源语言依据。',
    '失败案例则更加能够体现模型局限。包含专有名词的句子容易出现 <unk>，这是因为专有名词在训练集中频率低，且最小词频过滤会将低频 token 排除在词表外。对于“戈特福里德·休帕茨”等人名，模型既缺少足够训练样本，也缺少拷贝机制，因此难以正确输出。',
    '长句失败的原因主要是信息保持困难。虽然 GRU 可以建模序列信息，但当句子变长时，模型需要在隐藏状态中保留更多内容，解码阶段还要决定每一步关注哪里。基础 RNN 模型在这方面弱于 Transformer，因此长句中容易出现漏译、错译和重复生成。',
    '重复生成是另一个典型问题。例如模型可能反复输出 you 或 know。这通常与贪心解码、训练不足和隐藏状态更新不稳定有关。一旦某个高频词在某一步获得较高概率，贪心解码会直接选择它，后续状态又可能继续倾向该词，从而形成重复。Beam Search、长度惩罚或更强的解码约束可以缓解这一问题。',
    '从失败案例中可以看出，模型并不是完全随机输出，而是部分抓住了源句中的某些信息。例如含有“你”的句子可能生成 you，但没有正确组织完整句意。这说明模型具备局部词汇映射能力，但缺少全句语义组织能力。该现象正是基础 Seq2Seq 模型在小规模训练条件下的典型表现。'
]:
    para(doc, t)

heading(doc, 'A.6 与作业要求的逐项对应总结', 2)
for t in [
    '数据准备方面，项目选择了公开中英平行语料库 OPUS OpenSubtitles，并通过脚本将原始文本整理为 CSV，同时完成了空句过滤、过长句处理、重复样本去除和训练/验证/测试划分。',
    '模型实现方面，项目实现了编码器—解码器模型，并在解码器中集成 Bahdanau Attention。该实现满足题目中“若使用编码器-解码器模型需在解码器中集成并实现注意力机制”的要求。',
    '模型训练与调试方面，项目记录了每轮训练损失、验证损失和困惑度，并保存验证集表现最好的模型参数。损失曲线能够直观展示模型训练状态。',
    '量化评估方面，项目在测试集上计算 BLEU 分数，并给出了 1-gram 到 4-gram precision、BP、系统输出长度和参考长度。定性评估方面，项目导出了成功案例、失败案例和注意力热力图，并结合模型原理分析了失败原因。',
    '因此，本项目不是只提交一个可运行脚本，而是形成了包含方法简述、核心结果和深度分析的完整实验材料，符合课程项目对过程体验和模型行为分析的要求。'
]:
    para(doc, t)

doc.save(OUT)
print(OUT)
